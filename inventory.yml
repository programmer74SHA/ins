---
# NVIDIA GPU Infrastructure Inventory
# Update the IP addresses and ansible_user to match your environment

all:
  children:
    servers:
      hosts:
        gpu-server-01:
          ansible_host: 192.168.166.62
          ansible_user: root

      vars:
        ansible_python_interpreter: /usr/bin/python3
        ansible_become: yes
        app_environment: production

        # NVIDIA Driver Configuration
        nvidia_driver_interactive: false
        nvidia_driver_version: ""  # Empty for recommended driver
        nvidia_install_cuda: true
        nvidia_enable_persistence: true
        nvidia_reboot_after_install: true

    # Optional: Separate development/staging environments
    # gpu_dev:
    #   hosts:
    #     gpu-dev-01:
    #       ansible_host: 172.25.7.200
    #       ansible_user: ubuntu
    #   vars:
    #     ansible_python_interpreter: /usr/bin/python3
    #     ansible_become: yes
    #     app_environment: development
    #     nvidia_driver_interactive: true
    #     nvidia_install_cuda: true
    #     nvidia_reboot_after_install: false
