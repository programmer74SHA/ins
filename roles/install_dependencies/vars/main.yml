---
# vars file for install_dependencies
# These variables have higher priority than defaults

# Package state
package_state: present

# Docker specific versions (override if needed)
# docker_ce_version: "5:24.0.0-1~ubuntu.24.04"

# Additional packages (can be extended in playbook)
additional_packages: []
